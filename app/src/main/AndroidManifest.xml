<manifest xmlns:android="http://schemas.android.com/apk/res/android" package="com.tradeledger.offline">
    <application
        android:allowBackup="true"
        android:label="TradeLedger Offline"
        android:theme="@style/Theme.TradeLedgerOffline">
        <activity android:name=".MainActivity" android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>
</manifest>